<form method="post" action="{{ path('app_piste_admin_delete', {'id': piste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piste.id) }}">
    <button class="btn">Delete</button>
</form>
